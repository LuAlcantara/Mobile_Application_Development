import { View, StyleSheet } from "react-native";

export default (props) => {
    return(
        <View style={
            {
                width: 50,
                height: 50,
                backgroundColor:ProgressBarAndroidComponent.cor||"#000"
            }
        }>
        </View>
    )
}